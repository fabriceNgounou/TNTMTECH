@extends('layouts.app')
@section('title', $job->title.' — Carrières TNTMTECH')
@section('content')
<section class="page-head"><div class="container"><p class="eyebrow">{{ $job->city }} · {{ $job->contract_type }}</p><h1>{{ $job->title }}</h1><p>Candidatures ouvertes jusqu’au {{ optional($job->deadline)->format('d/m/Y') ?: 'pourvoi du poste' }}.</p></div></section>
<section class="section container form-layout"><div><h2>Le poste</h2><p class="lead">{{ $job->description }}</p><h2>Vos missions</h2><div class="program-list">@foreach($job->missions ?? [] as $i => $mission)<div><span>{{ $i + 1 }}</span><p>{{ $mission }}</p></div>@endforeach</div></div><form class="form-panel" method="post" action="{{ route('jobs.apply', $job) }}" enctype="multipart/form-data">@csrf<h2>Postuler</h2><label>Nom complet *<input name="name" required></label><div class="field-row"><label>Email *<input type="email" name="email" required></label><label>Téléphone *<input name="phone" required></label></div><label>Ville *<input name="city" required></label><label>Votre message<textarea name="message" rows="4"></textarea></label><label>CV * <small>PDF, DOC ou DOCX · 5 Mo max.</small><input type="file" name="cv" accept=".pdf,.doc,.docx" required></label><label class="check"><input type="checkbox" name="consent" required><span>J’accepte le traitement de ma candidature.</span></label><button class="btn btn-primary btn-block">Envoyer ma candidature</button></form></section>
@endsection
